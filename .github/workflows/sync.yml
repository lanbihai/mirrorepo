name: Sync images to Aliyun
on:
#  schedule:
#    - cron: '0 0 * * *'
  push:
    paths:
#      - 'example/variables.yaml'
      - 'images.txt'
jobs:
  quay-to-aliyun:
    runs-on: ubuntu-20.04
    container: setzero/registry-manager
    env:
      TO_USERNAME: ${{ secrets.TO_USERNAME }}
      TO_PASSWORD: ${{ secrets.TO_PASSWORD }}
    steps:
    - name: Quay to Aliyun
      run: |
        cat << EOF >config.yaml
        registries:
          aliyuncs:
            registry: https://registry.aliyuncs.com
            username: ${{ secrets.TO_USERNAME }}
            password: ${{ secrets.TO_PASSWORD }}
          quay:
            registry: https://quay.io
            repositories:
              - jetstack/cert-manager-ctl:v1.8.1
              - jetstack/cert-manager-cainjector:v1.8.1
              - jetstack/cert-manager-webhook:v1.8.1
              - jetstack/cert-manager-controller:v1.8.1
        sync-policy:
          from: quay
          to: aliyuncs
          dry-run: false
          replace:
            - old: jetstack/
              new: kubedog/jetstack_
          filters:
            - ^[Vv]?\d+\.\d+(\.\d+)?(-\d+)?$
        EOF
        registry-manager sync -c config.yaml
    - name: Delete workflow runs
      uses: GitRML/delete-workflow-runs@main
      with:
        retain_days: 7
        keep_minimum_runs: 3
  
